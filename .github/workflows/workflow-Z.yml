name: Workflow Z - Auto Update Workflow X References

on:
  push:
    paths:
      - '.github/workflows/workflow-X.yml'
    branches:
      - main
  workflow_dispatch:

jobs:
  update-references:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout common repo
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Get new commit hash
        id: hash
        run: |
          NEW_HASH=$(git rev-parse HEAD)
          echo "new_hash=${NEW_HASH}" >> $GITHUB_OUTPUT
          echo "New commit hash: ${NEW_HASH}"

      - name: Read repos config
        id: config
        run: |
          content=$(cat .github/repos-config.yml)
          echo "repos_config<<EOF" >> $GITHUB_OUTPUT
          echo "$content" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Install YQ for YAML parsing
        run: |
          sudo apt-get update
          sudo apt-get install -y yq

      - name: Parse and process repos
        env:
          NEW_HASH: ${{ steps.hash.outputs.new_hash }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Extract repo names from config using pipe
          repos=$(cat .github/repos-config.yml | yq eval '.repos[].name' -)
          
          for repo in $repos; do
            echo "=========================================="
            echo "Processing repository: $repo"
            echo "=========================================="
            
            # Get workflow file for this repo using pipe
            workflow_file=$(cat .github/repos-config.yml | yq eval ".repos[] | select(.name == \"$repo\") | .workflow_file" -)
            
            if [ -z "$workflow_file" ]; then
              echo "❌ Workflow file not found for $repo"
              continue
            fi
            
            echo "✅ Workflow file: $workflow_file"
            
            # Clone repository
            echo "📥 Cloning repository: $repo"
            git clone https://github.com/AshishGodse/$repo.git temp/$repo
            
            # Extract old hash from workflow file
            old_hash=$(grep -oP 'workflow-X\.yml@\K[a-f0-9]{40}' temp/$repo/$workflow_file 2>/dev/null || echo "")
            
            if [ -z "$old_hash" ]; then
              echo "❌ No workflow-X reference found in $repo"
              continue
            fi
            
            echo "📌 Current hash in $repo: $old_hash"
            echo "📌 New hash from common repo: $NEW_HASH"
            
            if [ "$old_hash" == "$NEW_HASH" ]; then
              echo "✅ Hash already up-to-date in $repo"
              continue
            fi
            
            echo "🔄 Updating $repo: $old_hash -> $NEW_HASH"
            
            # Update workflow file
            cd temp/$repo
            sed -i "s/${old_hash}/${NEW_HASH}/g" $workflow_file
            
            # Configure git
            git config user.email "automation@github.com"
            git config user.name "Automation Bot"
            
            # Create branch and commit
            branch_name="update-workflow-x-${NEW_HASH:0:8}"
            echo "🌿 Creating branch: $branch_name"
            git checkout -b $branch_name
            git add $workflow_file
            git commit -m "chore: update workflow-x reference to ${NEW_HASH:0:8}"
            
            # Push branch
            echo "📤 Pushing branch to origin"
            git push origin $branch_name
            
            # Create PR
            echo "📝 Creating Pull Request"
            gh pr create \
              --title "chore: update workflow-x reference to ${NEW_HASH:0:8}" \
              --body "Automated update for workflow-x commit reference\n\nOld hash: $old_hash\nNew hash: $NEW_HASH\n\nThis PR was automatically created by Workflow Z" \
              --base main \
              --head $branch_name || echo "⚠️ PR creation might have issues but branch is pushed"
            
            cd ../..
            echo "✅ Completed processing $repo"
            echo ""
          done
          
          echo "=========================================="
          echo "✅ All repositories processed successfully"
          echo "=========================================="
        shell: bash